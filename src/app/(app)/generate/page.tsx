import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function GeneratePage() {
  return (
    <PagePlaceholder
      title="콘텐츠 생성"
      description="클라이언트·채널 선택 → (스레드) 유형 선택 → 주제 입력 → 프리셋 기반 통합 생성 엔진으로 생성."
      phase="Phase 1 (5·6단계)"
    />
  );
}
