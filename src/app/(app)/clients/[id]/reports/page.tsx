import { GoogleReportView } from "@/components/reports/google-report-view";

/** 서치콘솔 · GA4 — 구글 결과만 불러와 정리 (주소는 /reports 그대로) */
export default function ClientReportsPage() {
  return <GoogleReportView />;
}
