import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function LibraryPage() {
  return (
    <PagePlaceholder
      title="라이브러리"
      description="생성된 콘텐츠 전체. 클라이언트·채널·기간·작성자 필터, 재복사, WP 발행 상태."
      phase="Phase 1 (9단계)"
    />
  );
}
