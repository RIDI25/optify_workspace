-- ============================================================
-- 옵티파이 워크스페이스 — contents.meta 추가 (0006)
-- 라이브러리 상세를 생성 결과 화면과 동일하게 렌더하기 위해
-- 워프 슬러그/메타/FAQ 등 패널 데이터를 저장한다. (이미지는 images 컬럼 재사용 — 객체 배열)
-- 멱등: 컬럼 없으면 추가.
-- ============================================================

alter table contents
  add column if not exists meta jsonb not null default '{}'::jsonb;
